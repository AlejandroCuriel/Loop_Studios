<nav class="container nav">
    <a href="<%= root_path %>" > 
        <img class="logo" src="<%= asset_path('logo.svg') %>" alt=""/>
    </a>
    <img class="icon-hamburger only-mobile" src="<%= asset_path('icon-hamburger.svg') %>" alt=""/>
    <div class="menu-list only-desktop">
        <!-- <%= link_to '' %> --> 
        <a href="<%= about_path %>">About</a>
        <a href="<%= products_path %>">Products</a>
        <a href="<%= contact_path %>">Contact</a>
    </div>
</nav>

